# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / Next.js
web/node_modules/
web/.next/
web/next-env.d.ts

# İşletim sistemi
.DS_Store

# İBB ham GeoJSON önbelleği (türetilmiş data/istanbul_metro_agi.json depoda tutulur)
data/harici/

# Türetilmiş birleşik veri (train + test'in birleşimi; hiçbir modül okumuyor,
# 'python src/rayli_veri_uret.py' ile yeniden üretilir) — depo boyutu için hariç
data/rayli_sistem_tum_veri.csv

# Çalışma anında oluşan kayıt veritabanı (SQLite)
data/rayli_kayit.db
web/tsconfig.tsbuildinfo

# NLP metin sınıflandırma modülü (nlp/) — kendi sanal ortamı, gizli anahtarları ve
# çalışırken büyüyen dosyaları. model/govde, data/seed|raw|processed BİLİNÇLİ OLARAK
# hariç değil (küçük, adımların asıl çıktısı — bkz. nlp/CLAUDE.md "Genel İlkeler" #6).
nlp/venv/
nlp/.env
nlp/data/logs.db
nlp/model/canli_kayip.json
nlp/.pytest_cache/
